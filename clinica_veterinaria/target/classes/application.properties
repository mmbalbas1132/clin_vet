# Configuraci�n de la conexi�n a la base de datos
# URL de la base de datos, se toma de la variable de entorno DB_URL
spring.datasource.url=${DB_URL}
# Usuario de la base de datos, se toma de la variable de entorno DB_USER_NAME
spring.datasource.username=${DB_USER_NAME}
# Contrase�a de la base de datos, se toma de la variable de entorno DB_PASSWORD
spring.datasource.password=${DB_PASSWORD}

# Nombre identificador de la aplicaci�n
spring.application.name=app_vet

# Configuraci�n de JPA/Hibernate
# Actualiza el esquema de la base de datos autom�ticamente
spring.jpa.hibernate.ddl-auto=update
# Dialecto de Hibernate para MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Puerto donde se expone la aplicaci�n
server.port=8080

# (Comentado) Plataforma de base de datos, puede usarse para personalizar el dialecto
#spring.jpa.database-platform=${DB_PLATFORM}
